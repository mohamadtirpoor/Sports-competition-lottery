# 🚀 راهنمای دیپلوی در لیارا - گام به گام

## مرحله 1: دیپلوی Backend (Node.js)

### 1. ساخت پروژه Backend در لیارا
1. وارد پنل لیارا شوید
2. روی "ایجاد برنامه" کلیک کنید
3. نام برنامه: `tournament-api` (یا هر نام دلخواه)
4. پلتفرم: **Node.js**
5. روی "ایجاد برنامه" کلیک کنید

### 2. دیپلوی Backend
```bash
cd backend
liara deploy --platform=node --port=5000
```

یا اگر Liara CLI ندارید:
```bash
npm install -g @liara/cli
liara login
cd backend
liara deploy --platform=node --port=5000
```

### 3. یادداشت کردن URL
بعد از دیپلوی، URL شما چیزی شبیه این خواهد بود:
```
https://tournament-api.liara.run
```

---

## مرحله 2: دیپلوی Frontend (Next.js)

### 1. تنظیم آدرس API
فایل `frontend/.env.production` را ویرایش کنید:
```env
NEXT_PUBLIC_API_URL=https://tournament-api.liara.run/api
```

**مهم**: `tournament-api` را با نام واقعی پروژه Backend خود جایگزین کنید!

### 2. ساخت پروژه Frontend در لیارا
1. وارد پنل لیارا شوید
2. روی "ایجاد برنامه" کلیک کنید
3. نام برنامه: `tournament-app` (یا هر نام دلخواه)
4. پلتفرم: **Next.js**
5. روی "ایجاد برنامه" کلیک کنید

### 3. تنظیم متغیر محیطی
در پنل لیارا:
1. وارد پروژه `tournament-app` شوید
2. تب "تنظیمات" → "متغیرهای محیطی"
3. اضافه کنید:
   - کلید: `NEXT_PUBLIC_API_URL`
   - مقدار: `https://tournament-api.liara.run/api`

### 4. دیپلوی Frontend
```bash
cd frontend
liara deploy --platform=next
```

---

## مرحله 3: تنظیم CORS در Backend

اگر خطای CORS دریافت کردید، فایل `backend/server.js` را ویرایش کنید:

```javascript
const cors = require('cors');

app.use(cors({
  origin: [
    'https://tournament-app.liara.run',  // آدرس Frontend خود را بگذارید
    'http://localhost:3000'  // برای تست محلی
  ],
  credentials: true
}));
```

سپس دوباره Backend را دیپلوی کنید:
```bash
cd backend
liara deploy
```

---

## ✅ چک‌لیست نهایی

- [ ] Backend دیپلوی شده و در دسترس است
- [ ] Frontend دیپلوی شده و در دسترس است
- [ ] متغیر `NEXT_PUBLIC_API_URL` در Frontend تنظیم شده
- [ ] CORS در Backend برای دامنه Frontend تنظیم شده
- [ ] سایت باز می‌شود و API کار می‌کند

---

## 🐛 رفع مشکلات رایج

### خطای 503 (Service Unavailable)
- بررسی کنید که پروژه در حال اجرا است (در پنل لیارا)
- لاگ‌ها را چک کنید: `liara logs`
- مطمئن شوید `package.json` دارای `start` script است

### خطای CORS
- CORS را در Backend تنظیم کنید (بالا را ببینید)
- مطمئن شوید آدرس Frontend صحیح است

### API کار نمی‌کند
- متغیر `NEXT_PUBLIC_API_URL` را چک کنید
- مطمئن شوید Backend در دسترس است
- در Console مرورگر خطاها را بررسی کنید

### دیتابیس خالی می‌شود
- برای دیسک دائمی در لیارا:
  1. تب "دیسک‌ها" → "افزودن دیسک"
  2. مسیر: `/data`
  3. در کد، دیتابیس را در `/data/tournament.db` ذخیره کنید

---

## 📝 دستورات مفید

```bash
# مشاهده لاگ‌ها
liara logs

# مشاهده لاگ‌های زنده
liara logs --follow

# ری‌استارت برنامه
liara restart

# لیست برنامه‌ها
liara app list

# حذف برنامه
liara app remove
```

---

## 🔗 لینک‌های مفید

- [مستندات لیارا - Node.js](https://docs.liara.ir/app-deploy/nodejs/getting-started/)
- [مستندات لیارا - Next.js](https://docs.liara.ir/app-deploy/nextjs/getting-started/)
- [مستندات لیارا - دیسک](https://docs.liara.ir/app-features/disks/)
